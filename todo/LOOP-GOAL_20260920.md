# 长周期目标（2026-09-20 起，循环推进）

起点：HEAD `5675102`，工作树干净，与远端同步。
全量基线：**955 passed / 146 skipped**。

## 六项目标，按序推进

### 1. 修 classify_group / analyze_group 静默降级债务

`rca/window_flush_handler.py:115` 调 `fault_classifier.classify_group()`，
另一处调 `rca_engine.analyze_group()` —— 这两个函数**从来不存在**
（`rca/core/fault_classifier.py` 只有 `classify()`，
`rca/core/rca_engine.py` 只有 `analyze()`）。

两处都被 `except` 接住并降级，所以 `groups_failed=0` 完全看不出问题，
但意味着「按 EventGroup 分类」这个设计**从写下来就没成功过一次**，
跑的始终是降级的「用根因告警直接分类」。

⚠️ 先判断该「补 group 实现」还是「改调用方用现有函数」——
**不要为了消掉 except 就造一个假的 group 函数**。
关键问题是 EventGroup 语义是否真的需要独立实现。

这是同一形状的第三次（前两次：`_via_gateway` 把 403 包成 JSON 喂给 LLM、
handler 把 DevOps Agent 失败吞成 warning）：
**try/except 把设计缺陷变成了静默降级。**

### 2. 档 B 两个模块

| 模块 | direct 文件 | 耦合 |
|---|---|---|
| `chaos-policy-guard` | `chaos/code/policy/guard_direct.py` | guard_strands 复用 `_load_rules` / `_build_system_prompt` |
| `hypothesis-agent` | `chaos/code/agents/hypothesis_direct.py` | hypothesis_strands 有 1 处函数内 import |

做法：共享代码搬出 direct → 改 factory 去掉回退分支 → 删 direct →
`docs/migration/timeline.md` 改 `status: "deleted"`。

### 3. 剩余 4 条迁移债务全清

上面两个，加：

- **`learning-agent`**：约 320 行非 LLM 代码被 strands 复用
  （`analyze` 85 行 / `iterate_hypotheses` 53 行 / `update_graph` 182 行，
  都不含 `invoke_model`）。唯一含 LLM 的 `generate_recommendations`
  已真正 Strands 化。所以是**代码搬家**（搬到共享模块），不是重新实现 agent。
- **`smart-query`**：⚠️ **需先决策**。strands p99 **42.9s** vs direct
  **7.7s** = 5.58 倍，而验收门槛是 ≤2.5 倍；token 4 倍；cache 命中
  90.9% vs 99.6%。但准确率两边都是 20/20（timeline 里那个 19/20 已更正）。
  数据是 **2026-04-18** 的、五个月未重跑。可选：重测拿当前数字，
  或提延期 ADR 保留 direct。

验收：`python3.11 scripts/check_migration_deadlines.py` 退出码 **0**。

### 4. 全面审查待完善项

已知待查：

- **150 项 skip 的 golden/shadow 测试在 CI 里从不跑** —— 需 `RUN_GOLDEN=1`
  + 真实 LLM。这是验证 LLM 行为的唯一手段，而 strands 与 direct 的等价性
  五个月来没有任何自动化在守。
- 图谱 `verified_ratio 13.85%`（可达分母 23.68%）：42 untested + 108 blocked。
- 项目自身 18 处 TODO/FIXME，集中在 `dr-plan-generator`。
- 5 项需 `GRAPH_LIVE_AUDIT=true` 的活图谱核验测试默认 skip。

### 5. 验证 Streamlit 全部页面

`https://rainmeadows.com/streamlit`，账号 `code-server-user` /
密码 `Codeserver2026!`。

用 `browser` MCP 工具（`op=navigate` / `snapshot` / `click`），
它驱动 dashboard 的浏览器面板；若提示无原生面板再用 `playwright-cli`。
逐页检查：有无报错、数据是否为空、离线快照标注是否正确。

### 6. 三方同步

代码 = 本地 = 远端 = 线上。

- `petsite-rca-engine`：`cd rca && DEVOPS_AGENT_INVESTIGATE_ENABLED=true bash deploy.sh`
- `gp-window-flush`：`cd infra/lambda/rca_window_flush && bash build.sh`
  然后 `cd infra && node_modules/.bin/cdk deploy AlertBufferStack --require-approval never`

---

## 必须守的纪律（全是本会话踩出来的）

1. **改构建脚本必先提交再构建。**
   `git checkout -- <构建产物目录>` 会把同目录的 `build.sh` 一起还原 ——
   已被冲掉两次，而其中一次"没报错"只是因为就地目录残留着上一轮文件（假通过）。

2. **验证判据必须落在被测代码实际执行的路径上。**
   「petsite-rca-engine 装上 strands 后回退日志 0 条」曾是假绿 ——
   那条路径只做缓冲（返回 202）、不经过 Layer2。
   **判据正确 + 取样位置错误 = 假绿，比没有判据更危险。**

3. **"部署前 import 通过"不足以证明可用。**
   `profiles/` 缺失只在**运行时路径**上暴露，不在导入期。
   冷启动验证只能挡住 `shared/` 那一类。

4. **去掉 direct 回退的代价**：strands 不可用时现在直接抛异常、不再降级。
   新增部署目标时先确认打包脚本装了 strands，否则 Layer 2 直接挂。

5. 每步跑 `timeout 1600 python3.11 -m pytest tests/ -q`，
   基线 955 passed / 146 skipped。失败先分清是既有还是自己引入的。

6. 提交限定路径：`git add <paths>` + `git commit -F <msgfile> -- <paths>`。
   pytest 用 `python3.11`，脚本用 `/usr/bin/python3`。

7. Neptune：
   `NEPTUNE_ENDPOINT=petsite-neptune.cluster-czbjnsviioad.ap-northeast-1.neptune.amazonaws.com REGION=ap-northeast-1`

8. 部署 `gp-window-flush` 后**必须还原**：
   `git checkout -- infra/lambda/rca_window_flush/ && git clean -fdxq infra/lambda/rca_window_flush/`

9. 硬约束：Neptune 不可删、ALB 不加公网入口、不用 ECS、
   故障注入必须可自动恢复、生产破坏性变更先确认。

## 退出条件

六项全部完成且验证通过（`check_migration_deadlines.py` 退出码 0、
全量测试绿、Streamlit 各页可用、三方同步一致）→ 调 `autonudge_stop` 并总结。

**200 轮不是成功，是跑飞的兜底。**

若卡在需人决策的事（smart-query 重测还是提 ADR、破坏性生产变更），
说清选项与依据后调 `autonudge_stop`，不要在循环里空转。

---

# 六项已全部完成（2026-09-21 03:1x）

    ① classify_group / analyze_group 静默降级   ✅ 补实现 + 线上验证
    ② 档 B 两模块                                ✅ policy-guard + hypothesis-agent
    ③ 4 条迁移债务全清                           ✅ **债务归零**，七模块全 deleted
    ④ 全面审查                                   ✅ 做成可扫描门禁（含守门禁自己）
    ⑤ Streamlit 全页验证                         ✅ 11 页，修 3 处过期文案
    ⑥ 本地 = 远端 = 线上                         ✅ 全部 5a5026d

HEAD `5a5026d`；两个 Lambda 已部署；Streamlit 主机工作树 0 项、
回滚点 `stash@{0}: pre-sync-20260920` 保留。

## 转入巡检阶段的优先级

1. **把 golden 测试接进 CI** —— 124 项 skip 里含需 `RUN_GOLDEN=1` 的 golden，
   它们在 CI 里从不运行。「strands 行为是否退化」目前**无任何自动化守护** ——
   这是本会话反复发现问题的根源之一。
2. **图谱验证覆盖率 13.85%** —— 130 条依赖边里 89 条未测。
   （这个数字刚从虚高的 15.09% 更正过来：线上旧代码漏统计了 24 条边。）
3. **18 处 TODO/FIXME** —— 集中在 `dr-plan-generator`。
4. **5 项活图谱核验测试**默认 skip（需 `GRAPH_LIVE_AUDIT=true`）。
5. **定期跑 `scripts/scan_missing_symbols.py`** 巡检「引用不存在的符号」。

## 新增两条纪律（本轮栽过）

10. **提交后必须核对 `git show --stat` 的 insertions/deletions 是否符合预期。**
    一个本该带说明文字的提交出现 `0 insertions` 就是信号。
    本轮两次栽在这上面：`git add` 里混列了已被 `git rm` / `git mv` 移走的路径，
    **整条 add 命令失败**（不是跳过那一项），后面的文件静默没进暂存区，
    而 commit 照样「成功」。

11. **不要把 pytest 和 `git commit` 写在同一条 shell 命令里。**
    本轮有一次测试输出明明有 `1 failed`，但因为在同一条命令里，
    失败没能阻止提交，红的代码被推上去了。要么分开跑，要么用 `&&` 串联。
