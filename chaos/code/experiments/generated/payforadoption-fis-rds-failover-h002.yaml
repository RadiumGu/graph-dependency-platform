# 由 HypothesisAgent 生成 — 2026-04-02 06:28
# 假设 H002: payforadoption Tier0 核心支付链路：RDS 主备故障转移韧性验证
# 故障域: data | 后端: fis
# FIS Action: aws:rds:failover-db-cluster
# 注意: extra_params 中 ${PLACEHOLDER} 需要替换为实际值

name: payforadoption-fis-rds-failover-h002-20260402
description: "payforadoption Tier0 核心支付链路：RDS 主备故障转移韧性验证"

target:
  service: payforadoption
  namespace: default
  tier: Tier0

fault:
  type: fis_rds_failover
  duration: "3m"
  extra_params:
    cluster_arn: "arn:aws:rds:ap-northeast-1:926093770964:cluster:grafana-aurora-mysql"

steady_state:
  before:
    - metric: success_rate
      threshold: ">= 95%"
      window: "1m"
  after:
    - metric: success_rate
      threshold: ">= 95%"
      window: "5m"
    - metric: latency_p99
      threshold: "< 5000ms"
      window: "5m"

stop_conditions:
  - metric: success_rate
    threshold: "< 50%"
    window: "30s"
    action: abort
  - metric: latency_p99
    threshold: "> 8000ms"
    window: "30s"
    action: abort

rca:
  enabled: true
  trigger_after: "30s"

graph_feedback:
  enabled: true
  edges:
    - Calls

backend: fis

options:
  max_duration: "10m"
  save_to_bedrock_kb: false
