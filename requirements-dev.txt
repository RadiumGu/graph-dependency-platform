# 测试与本地开发依赖
#
# ⚠️ Python 版本要求:>= 3.10
#   本仓库有 661 处 py3.10+ 联合类型语法(`list | None`、`dict | None` 等)。
#   注意这些是**运行时**求值失败(py3.9 报 TypeError),语法本身合法 ——
#   所以 py_compile 全过,但导入即崩,失败清单极具误导性。
#
#   实测同一 commit 上的差异:
#     py3.9.25   173 passed /  51 failed / 112 skipped / 90 errors
#     py3.11.15  292 passed /  17 failed / 138 skipped / 19 errors
#
#   Lambda 运行时是 py3.12,故生产不受影响 —— 这纯粹是本地/CI 可复现性问题。
#
# 安装:
#   python3.11 -m pip install -r requirements-dev.txt
# 运行:
#   python3.11 -m pytest tests/
#
# 版本用下限而非钉死,与仓库内 7 个组件级 requirements.txt 的惯例一致。

# ── 测试框架 ──
pytest>=7.0

# ── 被测代码的运行时依赖(测试需导入这些模块) ──
boto3>=1.34
requests>=2.28
pyyaml>=6.0
pydantic>=2.0
# chaos/code 用 structlog 做结构化日志。缺它一项就产生 36 个 collection error,
# 与真实缺陷混在一起,无法判断套件到底是什么状态。
structlog>=23.0.0

# ── 仅测试用 ──
# moto 5.0 才提供统一的 mock_aws 装饰器;moto 4.x 是分服务的 mock_s3/mock_sqs...
# tests/test_12_unit_etl_aws.py 用 `from moto import mock_aws`,
# 装 moto 4 会报 ImportError 而非版本错误,排查时容易误判为代码问题。
# 这是**硬约束**,不是偏好。
moto>=5.0
# tests/test_22_ui_streamlit.py 用 streamlit.testing.v1.AppTest 测 demo 页面。
# 体量较大;若只关心生产代码(infra/rca/chaos/dr-plan),可跳过该文件:
#   python3.11 -m pytest tests/ --ignore=tests/test_22_ui_streamlit.py
streamlit>=1.28
# demo/pages/1_Graph_Explorer.py 用 pyvis 渲染交互式图谱。
# 缺它会让 test_22 产生 8 个 error。
pyvis>=0.3

# ── 可选依赖(缺失时相关测试 skip,不会 fail) ──
# 这些**故意不放在必装列表**里:体量大、且只影响个别测试。
# 相关测试已改为缺失即 skip —— 恒红项的危害不是它本身,而是它训练所有人
# 忽略红色,真缺陷会跟着被忽略。
#
# strands-agents: rca 的 Layer2/NLQuery 有 strands 实现(engines/factory.py
#   按 LAYER2_ENGINE / NLQUERY_ENGINE 环境变量切换,不可用时 warning + 回退 direct)。
#   缺它则 tests/test_layer2_memory.py 与 tests/test_layer2_golden.py 的
#   strands 分支 skip。取消下面这行注释即可启用:
# strands-agents>=0.1
#
# ── 外部工具(非 pip 包) ──
# aws-cdk CLI —— tests/test_11_schema_consistency.py::test_s0_06_cdk_synth 需要。
#   安装: npm install -g aws-cdk   (需 Node.js 18+)
#   未安装时该测试 skip。
