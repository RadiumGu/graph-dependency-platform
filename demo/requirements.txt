# demo 应用依赖。
# 注意 pyyaml 与 pydantic 是新增的：
#   pyyaml   —— 首页/各页面从 profiles/graph_contract.yaml 现算节点/边/来源数量
#   pydantic —— profiles/schema.py 的 validate_profile 依赖它；缺失时
#               Smart Query 的 few-shot 面板会降级（已加保护，不再整页崩溃）
# ── 版本策略（门禁 tests/test_59_demo_requirements_importable.py）───────────
#
# **API 已证明不稳的钉死（==），其余给下限（>=）。**
# 不是「钉死更安全」—— 对 boto3 钉死是更危险。
#
#   streamlit         用到 width="stretch" / st.tabs / AppTest 等版本敏感 API；
#                     线上与本地必须同版本才谈得上复现（2026-09-06 为此把线上
#                     从旧版升到 1.62.0）。
#   boto3             **刻意不钉**：必须保持新，否则拿不到新服务 ——
#                     本项目用的 `aws devops-agent` 就需要较新的 botocore。
#                     钉死它是主动制造故障。
streamlit==1.62.0
boto3>=1.34.0
requests>=2.31.0
pandas>=2.0.0
pyyaml>=6.0
pydantic>=2.0
# ── 2026-09-13：pyvis / networkx / st-link-analysis 三个都删了 ──────────────
#
# 两个图谱页现在都用 `demo/components/graph_svg`：dagre 布局（本地 UMD 文件，
# 不装 Python 包）+ 自绘 SVG，跑在 Streamlit Components v2 内联组件里，
# 所以既不需要 pyvis 生成 HTML，也不需要 Cytoscape 封装做双向通信。
#
# 为什么不是「留着以防万一」：st-link-analysis 把 Cytoscape 的
# min-zoomed-font-size 硬编码为 10 且不暴露 font-size，fit 后缩放低于 0.625 时
# **节点标签会整体消失**，那是把这一页的核心信息弄丢；pyvis 则是单向的，
# 选中的节点拿不回 Python。留着它们只会让人以为还有第二条可走的路。
#
# networkx 是当年配合 pyvis 建图用的，现在图结构就是节点/边两个 list，用不上。
